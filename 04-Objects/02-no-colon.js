const radius = 2;
const color = "red";

// If the properties of an object were defined before the object, you can set them without a colon like so:
const apple = {
    radius,
    color,
};

console.log(apple.color, apple.radius);
